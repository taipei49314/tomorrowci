[project]
name = "python-runtime-break"
version = "0.1.0"
requires-python = ">=3.9"
description = "Fixture: passes on Python 3.9, fails on 3.10+ via removed stdlib import path"

[tool.pytest.ini_options]
testpaths = ["tests"]
